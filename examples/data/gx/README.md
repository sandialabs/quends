# GX Data

GX CSV outputs used by the DataStream and ensemble tutorials.

- `tprim_2_*.out.csv`: individual GX runs.
- `ensemble/tprim_2_*_{a,b,c,d}.out.csv`: grouped ensemble members.

The main tutorial observable is `HeatFlux_st`; some examples also inspect
`Phi2_t` and `Wg_st`.
